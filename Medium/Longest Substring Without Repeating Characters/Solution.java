if(hash[charAt]!=-1)
        {
        while(r<s.length())
        // Arrays.fill(hash, -1);
        // List<Integer> hash=new ArrayList<>();
        int ans=0;
        int r=0;
        int l=0;
    public int lengthOfLongestSubstring(String s) {
        int[] hash=new int[256];
        Arrays.fill(hash, -1);
            {