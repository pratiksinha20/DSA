}
            
            mx=Math.max(mx, count);
        if(n==0)
        {
            return 0;
        }
        return mx+1;
        if else(n==1)
        {
            return 1;
        }
        
    }
}