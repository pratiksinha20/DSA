temp=temp/10;
            }
            return true;
    }
                }
                    return false;
                {
                if(rem==0 || num%rem!=0)
                int rem=temp%10;
            {
            while(temp!=0)
    public List<Integer> selfDividingNumbers(int left, int 
    right) {
        List<Integer> l=new ArrayList<>();
        for(int i=left; i<=right; i++)
        {
            if(solve(i))